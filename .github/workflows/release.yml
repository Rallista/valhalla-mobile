name: Release Valhalla Mobile

on: 
  push:
    branches:
      - main

jobs:
  release-on-push:
    runs-on: macos-14
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: rymndhng/release-on-push-action@master
        with:
          bump_version_scheme: patch